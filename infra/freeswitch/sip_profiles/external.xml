<!--
  FreeSWITCH external SIP profile.

  Used by all BD telco trunks. Gateway files are imported from
  `sip_profiles/external/` (rendered per provider via the Jinja template in
  `infra/sip-trunks/_template.xml.j2`).

  The ``external`` context maps to ``dialplan/public/*.xml`` — inbound DIDs
  hit 00_livocall_inbound.xml. Outbound calls use the ``default`` context.
-->
<profile name="external">
  <aliases>
    <alias name="outbound"/>
  </aliases>
  <gateways>
    <!-- `X-PRE-PROCESS cmd="include" data="external/*.xml"` pulls in rendered trunks. -->
    <X-PRE-PROCESS cmd="include" data="external/*.xml"/>
  </gateways>
  <settings>
    <param name="user-agent-string" value="livocall/0.2"/>
    <param name="debug" value="0"/>
    <param name="sip-trace" value="false"/>
    <param name="rfc2833-pt" value="101"/>
    <param name="sip-port" value="5080"/>
    <param name="dialplan" value="XML"/>
    <param name="context" value="public"/>
    <param name="dtmf-duration" value="2000"/>
    <param name="inbound-codec-prefs" value="PCMU@20i"/>
    <param name="outbound-codec-prefs" value="PCMU@20i"/>
    <param name="inbound-codec-negotiation" value="greedy"/>
    <param name="inbound-late-negotiation" value="false"/>
    <param name="disable-transcoding" value="true"/>
    <param name="rtp-autoflush-during-bridge" value="true"/>
    <param name="rtp-rewrite-timestamps" value="true"/>
    <param name="hold-music" value="local_stream://moh"/>
    <param name="rtp-timer-name" value="soft"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="ext-rtp-ip" value="auto-nat"/>
    <param name="ext-sip-ip" value="auto-nat"/>
    <param name="apply-inbound-acl" value="livocall_trunks"/>
    <param name="auth-calls" value="false"/>
    <param name="accept-blind-reg" value="false"/>
    <param name="accept-blind-auth" value="false"/>
    <param name="inbound-reg-force-matching-username" value="false"/>
    <param name="force-register-domain" value="$${domain}"/>
    <param name="force-subscription-domain" value="$${domain}"/>
    <param name="force-register-db-domain" value="$${domain}"/>
    <param name="challenge-realm" value="auto_from"/>
    <param name="NDLB-broken-auth-hash" value="true"/>
    <param name="tls" value="false"/>
  </settings>
</profile>
